machine:
  timezone:
    Asia/Tokyo

dependencies:
  override:
    - git clone https://github.com/small-teton/MpegTsAnalyzer.git
    
test:
  override:
    - cd MpegTsAnalyzer
    - go test -bench . -benchmem